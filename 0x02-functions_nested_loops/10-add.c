#include "main.h"

/**
 * add - Function that adds two integers
 * @a: first integer passed as parameter
 * @b: second integer passed as parameter
 * Return: a + b
 */

int add(int a, int b)
{
	return (a + b);
}
