#include "main.h"

/**
 * _isdigit - checks whether a character is a digit or not
 * @charac: character to be tested
 * Return: 1 or 0 if otherwise
 */

int _isdigit(int charac)
{
	if ((charac >= 48) && (c <= 57))
	{
		return (1);
	}
	return (0);
}
