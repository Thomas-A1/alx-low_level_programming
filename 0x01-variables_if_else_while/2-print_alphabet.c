#include <stdio.h>

/**
 * main - Entry of the program
 * Description - Printing the lowercase characters
 * Return: 0
 */

int main(void)
{
	char q;
	for (q = 'a'; character <= 'z'; character++)
		putchar(q);
	putchar('\n');
	return (0);

}
